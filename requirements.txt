# Use PyTorch's official index for reliable CPU builds
--extra-index-url https://download.pytorch.org/whl/cpu

# Streamlit and core dependencies
streamlit
Pillow
requests

# PyTorch (Updated to a slightly newer, highly compatible version)
torch==2.3.1
torchvision==0.18.1
torchaudio==2.3.1

# Transformers and Hugging Face ecosystem
transformers
huggingface-hub
safetensors
tokenizers

# TensorFlow (Let pip find a compatible version)
tensorflow

# PyTorch Lightning
pytorch-lightning

# Common numerical and utility libraries
pandas
PyYAML

# Add any other absolutely essential libraries from pytorch-stable-diffusion-main below
# Examples: einops, omegaconf