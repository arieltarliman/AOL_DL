# Core dependencies to match your local environment
streamlit==1.44.1
Pillow==11.1.0

# TensorFlow to match the exact version you used to save the models
tensorflow==2.10.0

# Common numerical and utility libraries (pinned to your local versions)
numpy==1.23.5
pandas==2.2.3
PyYAML==6.0.2
